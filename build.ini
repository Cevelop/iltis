[logging]
level = INFO
format = %%(asctime)s - %%(levelname)s: %%(message)s

[project]
name = ILTIS
prefix = ch.hsr.ifs.iltis

[mappings]
root = root
features = features
bundles = bundles
releng = releng
tests = tests

[build]
target = Target
