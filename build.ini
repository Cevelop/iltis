[logging]
level = INFO
format = %%(asctime)s - %%(levelname)s: %%(message)s

[project]
name = ILTISTestingCore
prefix = ch.hsr.ifs.iltis.testing.core

[mappings]
root = root
features = features
bundles = bundles
releng = releng
tests = tests

[build]
target = Target
