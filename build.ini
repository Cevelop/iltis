[logging]
level = INFO
format = %%(asctime)s - %%(levelname)s: %%(message)s

[project]
name = ILTISTestingTools
prefix = ch.hsr.ifs.iltis.testing.tools

[mappings]
root = root
features = features
bundles = bundles
releng = releng
tests = tests

[build]
target = Target
