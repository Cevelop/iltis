[logging]
level = INFO
format = %%(asctime)s - %%(levelname)s: %%(message)s

[project]
name = ILTISTestingHighlevel
prefix = ch.hsr.ifs.iltis.testing.highlevel

[mappings]
root = root
features = features
examples = examples
bundles = bundles
releng = releng
tests = tests

[build]
target = Target
